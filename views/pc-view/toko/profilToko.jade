//
   Created by riansyahPC on 11/27/2015.

extends ../layoutBelanja
append content
      .col-sm-1
      .row
         .col-sm-1(style="float:left;padding-right:220px "): img(src='/images/#{toko.logo}')
         .col-sm-7(style="float:left")
            .row
               h2 #{toko.nama}
               p #{toko.deskripsi}
            .row
               .col-sm-5
                  a.btn.btn-primary(href='/toko/jadikanfavorit/#{toko.id}') jadikan favorit
               .col-sm-7
                  center
                     h4 informasi toko
                     .col-sm-4 <h4>#{toko.Kabupaten.nama}</h4> lokasi
                     .col-sm-4 <h4>#{produkTerjual}</h4> produk terjual
      .row <br/>
         .features_items
            script.
               function numberFormat(_number, _sep) {
                  _number = typeof _number != "undefined" && _number > 0 ? _number : "";
                  _number = _number.replace(new RegExp("^(\\d{" + (_number.length % 3 ? _number.length % 3 : 0) + "})(\\d{3})", "g"), "$1 $2").replace(/(\d{3})+?/gi, "$1 ").trim();
                  if (typeof _sep != "undefined" && _sep != " ") {
                     _number = _number.replace(/\s/g, _sep);
                  }
                  return _number;
               }
            // features_items
            h2.title.text-center Produk Milik Toko #{toko.nama}
            .col-sm-1
            .col-sm-2(style='width:250px')
               .left-sidebar
                  #accordian.panel-group.category-products
                     .panel-default
                        ul.nav.nav-list
                           li.nav-header: h4 Etalase
                           li: a(href='/toko/profil/#{toko.id}#etalase') Semua Etalase
                           each value in etalase
                              li: a(href='/toko/profil/#{toko.id}/#{value.id}#etalase') #{value.nama}
            .col-sm-7
               block produkMilikEtalase
               //h3#etalase Semua Etalase <br/><br/>
               //each value,indexProduk in toko.Produk
               //   div(style='width:160px;height:270px;float:left')
               //      | <a href='/produk/detail/#{value.id}'>
               //      | <img src='/images/produk/#{value.gambar}', style='height: 150px;width:140px'/> <br/> </a>
               //      | <h4> Rp <span class='uang'> </h4>
               //      | <a href='/produk/detail/#{value.id}'> #{value.nama} </a><br/>
               //   script.
               //      document.getElementsByClassName("uang")[#{indexProduk}].innerHTML = numberFormat("#{value.harga}", ".");

